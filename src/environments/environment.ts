// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  HOUSES_API: 'http://demo.interfacema.de/programming-assessment-1.0/buildings',
  GOOGLE_MAPS_API_URL: 'https://maps.googleapis.com/maps/api/directions/json?',
  GOOGLE_MAPS_STATIC_MAP_API_URL: 'https://maps.googleapis.com/maps/api/staticmap?',
  API_KEY: 'AIzaSyC-d6BEE-N_Ej0pPYkb-wnhmKfmPYK1GRo'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
